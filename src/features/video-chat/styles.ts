/**
 * External dependencies.
 */
import { Platform, StyleSheet } from 'react-native';

export default StyleSheet.create({
    container: {
        flex: 1,
    },
    view: {
        zIndex: 2,
        width: 120,
        height: 120,
    },
});
