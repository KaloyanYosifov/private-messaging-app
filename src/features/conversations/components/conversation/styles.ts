/**
 * External dependencies.
 */
import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    listItemTitle: {
        fontSize: 16,
    },
    listItemDescription: {
        fontSize: 14,
    },
});
