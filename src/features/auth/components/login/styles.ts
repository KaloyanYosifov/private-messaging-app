/**
 * External dependencies.
 */
import formStyles from '@/features/auth/styles/form';
import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    ...formStyles,
});

export default styles;
